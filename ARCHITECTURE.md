# FaceRelis - Clean Architecture Implementation

## 🏗️ Архитектура

Проект FaceRelis переписан с использованием чистой архитектуры для улучшения поддерживаемости, тестируемости и масштабируемости.

### Структура проекта

```
face_relis/
├── app/                          # Основное приложение
│   ├── __init__.py
│   ├── main.py                   # FastAPI приложение
│   ├── core/                     # Ядро системы
│   │   ├── __init__.py
│   │   ├── config.py             # Конфигурация
│   │   ├── logging.py            # Логирование
│   │   └── exceptions.py         # Кастомные исключения
│   ├── api/                      # API слой
│   │   ├── __init__.py
│   │   ├── routes/               # HTTP роуты
│   │   │   ├── __init__.py
│   │   │   ├── files.py          # Файловые операции
│   │   │   ├── clustering.py     # Кластеризация
│   │   │   └── tasks.py          # Управление задачами
│   │   └── dependencies/         # DI контейнер
│   │       ├── __init__.py
│   │       └── ...               # Сервис провайдеры
│   ├── services/                 # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── clustering/           # Сервисы кластеризации
│   │   │   ├── __init__.py
│   │   │   ├── base.py           # Абстрактный интерфейс
│   │   │   └── local_service.py  # Локальная кластеризация
│   │   ├── face_detection/       # Детекция лиц
│   │   │   ├── __init__.py
│   │   │   ├── base.py           # Абстрактный интерфейс
│   │   │   └── insightface_service.py
│   │   └── file_service/         # Файловые операции
│   │       ├── __init__.py
│   │       ├── base.py           # Абстрактный интерфейс
│   │       └── local.py          # Локальная файловая система
│   ├── models/                   # Модели данных
│   │   ├── __init__.py
│   │   ├── domain.py             # Доменные модели
│   │   └── schemas.py            # API схемы (Pydantic)
│   └── utils/                    # Утилиты
│       ├── __init__.py
│       ├── file_utils.py         # Файловые утилиты
│       └── image_utils.py        # Утилиты изображений
├── tests/                        # Тесты
│   ├── __init__.py
│   ├── conftest.py               # Pytest конфигурация
│   ├── unit/                     # Модульные тесты
│   └── integration/              # Интеграционные тесты
├── scripts/                      # Вспомогательные скрипты
│   ├── setup/
│   ├── deployment/
│   └── maintenance/
├── frontend/                     # Фронтенд (будущий)
│   └── templates/
├── static/                       # Статические файлы
├── requirements.txt              # Основные зависимости
├── requirements-dev.txt          # Зависимости для разработки
├── main_new.py                   # Точка входа нового приложения
└── README.md
```

## 🧪 Запуск и тестирование

### Установка зависимостей

```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

### Запуск приложения

```bash
python main_new.py
```

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Только модульные тесты
pytest tests/unit/

# Только интеграционные тесты
pytest tests/integration/
```

### Линтинг и форматирование

```bash
# Форматирование кода
black app/ tests/
isort app/ tests/

# Проверка линтера
flake8 app/ tests/

# Проверка типов
mypy app/
```

## 🏛️ Принципы архитектуры

### 1. **Чистая архитектура (Clean Architecture)**

- **Entities (Domain Models)**: Бизнес-сущности в `models/domain.py`
- **Use Cases (Services)**: Бизнес-логика в `services/`
- **Interface Adapters (API)**: HTTP API в `api/routes/`
- **Frameworks & Drivers**: FastAPI, файловые операции

### 2. **SOLID принципы**

- **Single Responsibility**: Каждый модуль имеет одну ответственность
- **Open/Closed**: Код открыт для расширения, закрыт для изменений
- **Liskov Substitution**: Интерфейсы позволяют замену реализаций
- **Interface Segregation**: Специфические интерфейсы для каждого сервиса
- **Dependency Inversion**: Зависимости от абстракций, не от конкретных реализаций

### 3. **Инверсия зависимостей (DI)**

```python
# app/api/dependencies/__init__.py
def get_file_service() -> FileService:
    return LocalFileService()  # Можно заменить на другую реализацию
```

## 🔧 Основные компоненты

### Конфигурация

```python
# app/core/config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    host: str = "0.0.0.0"
    port: int = 8000
    debug: bool = False
    # ... другие настройки
```

### Логирование

```python
# app/core/logging.py
import structlog

logger = structlog.get_logger(__name__)
logger.info("Application started", user="admin")
```

### Сервисы

```python
# app/services/file_service/base.py
class FileService(ABC):
    @abstractmethod
    async def get_folder_contents(self, path: Path) -> List[FolderInfo]:
        pass
```

### API Роуты

```python
# app/api/routes/files.py
@router.get("/folder/{path:path}")
async def get_folder_info(path: str) -> List[FolderInfoResponse]:
    file_service = get_file_service()
    contents = await file_service.get_folder_contents(Path(path))
    return [FolderInfoResponse.from_domain(item) for item in contents]
```

## 🧪 Тестирование

### Модульные тесты

```python
# tests/unit/test_file_service.py
@pytest.mark.asyncio
async def test_get_folder_contents(temp_dir: Path, file_service: LocalFileService):
    # Arrange
    (temp_dir / "test.txt").write_text("content")

    # Act
    contents = await file_service.get_folder_contents(temp_dir)

    # Assert
    assert len(contents) == 1
    assert contents[0].name == "test.txt"
```

### Интеграционные тесты

```python
# tests/integration/test_api.py
@pytest.mark.asyncio
async def test_get_folder_api(client, temp_dir):
    # Тестирование полного API
    response = await client.get(f"/api/folder/{temp_dir}")
    assert response.status_code == 200
```

## 🔄 Миграция с legacy кода

### План миграции:

1. ✅ **Этап 1**: Базовая архитектура (текущий этап)
   - Структура папок
   - Базовые интерфейсы
   - Конфигурация и логирование
   - Простые сервисы

2. 🔄 **Этап 2**: Миграция бизнес-логики
   - Перенос кластеризации из `cluster_simple.py`
   - Интеграция с существующими ML моделями
   - Обновление API роутов

3. 🔄 **Этап 3**: Фронтенд и интеграция
   - Модернизация JavaScript кода
   - WebSocket для реального времени
   - Docker контейнеризация

4. 🔄 **Этап 4**: Оптимизация и развертывание
   - Кэширование и асинхронность
   - CI/CD пайплайн
   - Мониторинг и метрики

### Совместимость

Новый код обратно совместим со старым. Старое приложение продолжает работать в `main.py`, новое - в `main_new.py`.

## 📊 Метрики качества

### Цели после рефакторинга:

- **Тестируемость**: > 80% покрытие кода
- **Поддерживаемость**: < 10 мин на добавление новой фичи
- **Производительность**: < 2 сек на кластеризацию 1000 фото
- **Надежность**: < 0.1% ошибок в продакшене

## 🤝 Contributing

1. Следуйте принципам чистой архитектуры
2. Добавляйте тесты для нового кода
3. Используйте type hints
4. Форматируйте код с black и isort
5. Добавляйте документацию для публичных API
